import 'package:flutter/material.dart';

class UploadMeterPage extends StatelessWidget {
  const UploadMeterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Upload Meter Page'),
      ),
    );
  }
}