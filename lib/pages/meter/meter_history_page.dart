import 'package:flutter/material.dart';

class MeterHistoryPage extends StatelessWidget {
  const MeterHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Meter History Page'),
      ),
    );
  }
}