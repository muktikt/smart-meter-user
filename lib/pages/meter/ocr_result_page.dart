import 'package:flutter/material.dart';

class OcrResultPage extends StatelessWidget {
  const OcrResultPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Ocr Result Page'),
      ),
    );
  }
}